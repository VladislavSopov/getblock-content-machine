# GetBlock Content Machine

Exported from Ardor Cloud.

- `frontend/` — React + Tailwind UI service
- `backend-original/` — FastAPI backend service

The original service Git history is represented by the exported source branches in the Ardor environment.
